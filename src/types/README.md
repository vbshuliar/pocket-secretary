# `src/types`

This module owns shared application schemas and types.

Suggested first types:

- `NormalizedBotRequest`
- `TranscriptionResult`
- `AgentAction`
- `PendingConfirmation`
- `ContactMatch`
- `IntegrationError`
